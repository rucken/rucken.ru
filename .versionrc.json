{
    "scripts": {
        "postbump": "npm run release:version-writer && npm run generate && git add . && git commit -m \"chore: update children versions\""
    },
    "bumpFiles": [
        "package.json",
        "package-lock.json",
        {
            "filename": "VERSION.txt",
            "type": "plain-text"
        }
    ]
}
